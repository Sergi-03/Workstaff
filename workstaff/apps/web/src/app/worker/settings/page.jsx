import { SettingsPage } from "@/components/settingsPage";

export default function WorkerSettings() {
  return <SettingsPage />;
}

export const metadata = {
  title: "Configuración | WorkStaff",
  description: "Administra la configuración de tu cuenta en un solo lugar.",
};